export default function AuditLoading() {
  // Minimal skeleton — avoid spinner that blocks rendering on mobile.
  // The actual form is fully client-side and renders immediately.
  return (
    <div className="min-h-screen bg-zinc-950" />
  );
}
